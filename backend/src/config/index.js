// Configuration for backend (non-sensitive defaults)
module.exports = {
  mongoUri: 'mongodb://localhost:27017/dpr_evaluator',
  // If you deploy, set MONGO_URI in env instead of editing this file
};